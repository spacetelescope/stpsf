[tox]
envlist =
    py{312,313,314}
    py{312,313,314}-{poppydev,pysiafdev,astropydev}
    py312-legacy
    py{312,313,314}-{poppydev,pysiafdev}-cov
    py312-docbuild

isolated_build = true

[testenv]
passenv = *
extras =
    test
deps =
    pytest-remotedata   # ignore tests that use remote data, e.g. MAST, when running in CI
    poppydev,astropydev: git+https://github.com/spacetelescope/poppy.git#egg=poppy
    pysiafdev,astropydev: git+https://github.com/spacetelescope/pysiaf.git#egg=pysiaf
    astropydev: astropy>=0.0.dev0
    poppydev: synphot
    cov: pytest-cov
    xdist: pytest-xdist
uv_resolution =
    legacy: lowest-direct
    !legacy: highest
commands=
    pytest \
    cov: --cov-config=pyproject.toml --cov-report=xml --cov=stpsf \
    xdist: -n=2 --dist=loadscope \
    {posargs}

[testenv:docbuild]
passenv= *
extras =
    docs
commands=
    sphinx-build docs docs/_build

[testenv:codestyle]
skip_install = true
description = check package code style
deps =
    pycodestyle
commands =
    pycodestyle stpsf

[testenv:flake8]
exclude= .git,__pycache__, test*
max-line-length = 120
